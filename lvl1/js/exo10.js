let reserdWordsToCreateVariableJS = ["var", "let", "const"];

//Code à faire ici
console.log(typeof reserdWordsToCreateVariableJS);
let text = "Ceci est un tableau ça permet de de ranger des elements dans le but de plus facilement effectuer des manipulation sur les données.";